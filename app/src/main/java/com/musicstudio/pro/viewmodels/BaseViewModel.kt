package com.musicstudio.pro.viewmodels

import androidx.lifecycle.ViewModel

open class BaseViewModel : ViewModel() {
    // Shared logic for view models can be placed here (e.g., common error handling)
}
